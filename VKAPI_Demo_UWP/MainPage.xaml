<Page
    x:Class="VKAPI_Demo_UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VKAPI_Demo_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="uid" Margin="6" PlaceholderText="User id"/>
            <PasswordBox x:Name="token" Grid.Column="1" Margin="6" PlaceholderText="Access token"/>
            <Button Grid.Column="2" Margin="6" Content="Set" Click="SetToken"/>
        </Grid>
        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <Button Margin="6" Content="captcha.force" Click="Captcha_Force"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
